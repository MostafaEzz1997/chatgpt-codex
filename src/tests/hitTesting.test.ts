import { describe,it,expect } from 'vitest'; import { hitTestFurniture } from '../geometry/hitTest'; import { Room } from '../domain/models/Room';
const room:Room={id:'room',name:'R',widthCm:400,heightCm:300,openings:[],walkingPaths:[],furniture:[{id:'rect',type:'r',category:'c',name:'Rect',xCm:0,yCm:0,widthCm:50,depthCm:50,rotationDeg:0,color:'#000',shapeType:'rect'},{id:'l',type:'l',category:'c',name:'L',xCm:100,yCm:0,widthCm:100,depthCm:100,rotationDeg:0,color:'#000',shapeType:'compound',parts:[{xCm:0,yCm:0,widthCm:100,heightCm:30},{xCm:0,yCm:0,widthCm:30,heightCm:100}]}]};
describe('hit testing',()=>{it('hit-tests rectangular furniture',()=>expect(hitTestFurniture(room,{xCm:10,yCm:10})?.id).toBe('rect'));it('hit-tests L-shaped visible parts only',()=>{expect(hitTestFurniture(room,{xCm:190,yCm:90})?.id).not.toBe('l');expect(hitTestFurniture(room,{xCm:120,yCm:80})?.id).toBe('l');});});
