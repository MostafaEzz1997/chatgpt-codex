import React from 'react'; import { ScrollView, Text } from 'react-native'; import AppButton from '../components/ui/AppButton'; import { activeProject, activeRoom, usePlannerStore } from '../store/plannerStore'; import { exportProjectJson, importProjectJson } from '../storage/importExport'; import { exportRoomImageAsync } from '../storage/imageExport';
export default function ExportScreen(){const s=usePlannerStore(); const p=activeProject(s), room=activeRoom(s); const json=exportProjectJson(p); return <ScrollView style={{padding:16}}><Text style={{fontWeight:'900',fontSize:22}}>Export / Import</Text><AppButton title="Validate exported JSON" onPress={()=>importProjectJson(json)}/><AppButton title="Export current room image" onPress={()=>exportRoomImageAsync(room)}/><Text selectable>{json}</Text></ScrollView>}
