import React from 'react'; import AppBottomSheet from '../ui/AppBottomSheet'; import AppButton from '../ui/AppButton'; import { Room } from '../../domain/models/Room';
export default function RoomSettingsSheet({visible,onClose,room,settings,store}:{visible:boolean;onClose:()=>void;room:Room;settings:any;store:any}){return <AppBottomSheet visible={visible} title="Room Settings" onClose={onClose}><AppButton title={`Room +20 cm (${room.widthCm}×${room.heightCm})`} onPress={()=>store.updateRoomDimensions(room.widthCm+20,room.heightCm+20)}/><AppButton title={`Grid ${settings.showGrid?'on':'off'}`} onPress={store.toggleGrid}/><AppButton title={`Snap ${settings.snapToGrid?'on':'off'}`} onPress={store.toggleSnapToGrid}/><AppButton title="Grid size +10" onPress={()=>store.setGridSize(settings.gridSizeCm+10)}/><AppButton title={`Dimensions ${settings.showFurnitureDimensions?'on':'off'}`} onPress={store.toggleFurnitureDimensions}/><AppButton title={`All clearances ${settings.showAllClearances?'on':'off'}`} onPress={store.toggleAllClearances}/><AppButton title={`Walking paths ${settings.showWalkingPaths?'on':'off'}`} onPress={store.toggleWalkingPaths}/></AppBottomSheet>}
