import React from 'react'; import { Text, Pressable } from 'react-native'; import AppBottomSheet from '../ui/AppBottomSheet'; import AppButton from '../ui/AppButton'; import { furnitureLibrary } from '../../planner/furnitureLibrary';
export default function FurnitureLibrarySheet({visible,onClose,onAdd,onCustom}:{visible:boolean;onClose:()=>void;onAdd:(type:string)=>void;onCustom:()=>void}){return <AppBottomSheet visible={visible} title="Furniture Library" onClose={onClose}>{Object.entries(furnitureLibrary).map(([cat,items])=><React.Fragment key={cat}><Text style={{fontWeight:'800',marginTop:8}}>{cat}</Text>{items.map(i=><Pressable key={i.type} onPress={()=>onAdd(i.type)} style={{padding:10,borderBottomWidth:1,borderColor:'#eee'}}><Text>{i.name} — {i.widthCm}×{i.depthCm} cm</Text><AppButton title="Add" onPress={()=>onAdd(i.type)}/></Pressable>)}</React.Fragment>)}<AppButton title="Add custom 100×50" onPress={onCustom}/></AppBottomSheet>}
